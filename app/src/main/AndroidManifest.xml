<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.Reports"
        tools:targetApi="31">
        <activity
            android:name=".UpdateActivity"
            android:exported="true"
            android:label="Edit Report"
            android:parentActivityName=".ShowActivity"/>
        <activity
            android:name=".ShowActivity"
            android:exported="true"
            android:label="Show Reports"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".AddActivity"
            android:exported="true"
            android:label="Add Report"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".TodayActivity"
            android:exported="true"
            android:label="today's work"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Reports">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>